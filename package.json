{
  "devDependencies": {
    "cypress": "^14.4.1",
    "cypress-mochawesome-reporter": "^3.8.2",
    "cypress-multi-reporters": "^2.0.5",
    "mocha-junit-reporter": "^2.2.1"
  },
  "scripts": {
    "cy:open:chrome": "npx cypress open --browser chrome",
    "cy:run:chrome": "npx cypress run --browser chrome",
    "cy:open:dev": "npx cypress open --config-file cypress-dev.json --browser chrome",
    "cy:open:qa": "npx cypress open --config-file cypress-qa.json --browser chrome"
  }
}
